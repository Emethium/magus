<% provide(:title, 'Enviar SMS') %>
<h1>Envie o seu SMS aqui!</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@message) do |m| %>
      <%= m.label :número %>
      <%= m.text_field :number%>

      <%= m.label :mensagem %>
      <%= m.text_field :msg, :rows => 2 %>

      <%= m.submit "Enviar mensagem!", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
